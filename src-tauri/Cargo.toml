[package]
name = "elira-desktop"
version = "0.2.0"
description = "Elira AI desktop shell"
authors = ["Elira AI"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "1", features = [] }

[dependencies]
serde_json = "1"
tauri = { version = "1", features = [ "shell-open", "http-all"] }
