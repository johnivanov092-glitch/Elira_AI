# Elira AI core requirements (minimal — chat + tools + basic search)
# Install core:
#   cd backend && .venv\Scripts\pip install -r requirements.txt
# Install optional capabilities:
#   cd backend && .venv\Scripts\pip install -r requirements-optional.txt

# Core runtime
python-dotenv>=1.0
fastapi>=0.115,<0.116
uvicorn[standard]>=0.32,<0.35
pydantic>=2.10,<3.0
ollama>=0.4,<0.5
httpx>=0.27,<0.29
python-multipart>=0.0.12

# Numerics — RAG vector math (embeddings stored/compared as numpy buffers)
numpy>=1.26,<3

# Search + lightweight parsing
ddgs>=7.0
requests>=2.32
beautifulsoup4>=4.12
pypdf>=5.0

# Documents (lightweight only — heavy parsers live in optional)
python-docx>=1.1

# Secrets / file encryption
cryptography>=43.0

# File watching (realtime auto-index of the code-agent project)
watchdog>=4.0,<7.0

# Heavy capabilities (image-gen, OCR, Excel/PDF tables, pandas, screenshots)
# live in requirements-optional.txt and load lazily when first used.
